# H2 in-memory for tests
spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop

# Liquibase disabled for tests (H2 schema managed by Hibernate DDL)
spring.liquibase.enabled=false

# JWT configuration for tests (required by JwtService / @WebMvcTest slices)
app.jwt.secret=testSecretKeyForJwtThatIsAtLeast256BitsLongToAvoidWeakKeyWarning
app.jwt.expiration-ms=86400000
app.cors.allowed-origins=http://localhost:4200

# Mail configuration for tests (no external SMTP)
spring.mail.host=localhost
spring.mail.port=2525
spring.mail.username=test
spring.mail.password=test
spring.mail.properties.mail.smtp.auth=false
spring.mail.properties.mail.smtp.starttls.enable=false
app.mail.from=test@example.com
